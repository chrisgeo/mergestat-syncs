name: Build Docker images

on:
  push:
    tags:
      - 'v*'
  release:
    types:
      - published
  workflow_dispatch:
    inputs:
      version:
        description: 'Image tag to push (defaults to `latest`)'
        required: false
        default: 'latest'
      image_registry:
        description: 'Override the image registry (defaults to `ghcr.io/chrisgeo/dev-health-ops`)'
        required: false

env:
  IMAGE_REGISTRY: ghcr.io/chrisgeo/dev-health-ops

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  build-and-push:
    name: Build and push runner + API images
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Determine version
        run: |
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          if [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" ]]; then
            INPUT_VER="${{ github.event.inputs.version }}"
            if [[ "${INPUT_VER}" == "latest" ]]; then
              echo "PYTHON_VERSION=0.0.0+${SHORT_SHA}" >> $GITHUB_ENV
              echo "DOCKER_TAG=latest" >> $GITHUB_ENV
            else
              # Manual run: Docker tag with hyphen, Python version with plus
              echo "PYTHON_VERSION=${INPUT_VER}+${SHORT_SHA}" >> $GITHUB_ENV
              echo "DOCKER_TAG=${INPUT_VER}-${SHORT_SHA}" >> $GITHUB_ENV
            fi
            if [[ -n "${{ github.event.inputs.image_registry }}" ]]; then
              echo "IMAGE_REGISTRY=${{ github.event.inputs.image_registry }}" >> $GITHUB_ENV
            fi
          elif [[ "${GITHUB_EVENT_NAME}" == "release" ]]; then
            RELEASE_TAG="${{ github.event.release.tag_name }}"
            echo "PYTHON_VERSION=${RELEASE_TAG}" >> $GITHUB_ENV
            echo "DOCKER_TAG=${RELEASE_TAG}" >> $GITHUB_ENV
          elif [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            echo "PYTHON_VERSION=${GITHUB_REF_NAME}" >> $GITHUB_ENV
            echo "DOCKER_TAG=${GITHUB_REF_NAME}" >> $GITHUB_ENV
          else
            # Branch push: Use SHA for Docker tag, PEP 440 for Python
            echo "PYTHON_VERSION=0.0.0.dev0+${SHORT_SHA}" >> $GITHUB_ENV
            echo "DOCKER_TAG=${SHORT_SHA}" >> $GITHUB_ENV
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push runner image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile
          target: runner
          build-args: |
            SETUPTOOLS_SCM_PRETEND_VERSION=${{ env.PYTHON_VERSION }}
          tags: |
            ${{ env.IMAGE_REGISTRY }}/dev-hops-runner:${{ env.DOCKER_TAG }}
            ${{ env.IMAGE_REGISTRY }}/dev-hops-runner:latest
          push: true

      - name: Build and push API image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile
          target: api
          build-args: |
            SETUPTOOLS_SCM_PRETEND_VERSION=${{ env.PYTHON_VERSION }}
          tags: |
            ${{ env.IMAGE_REGISTRY }}/dev-hops-api:${{ env.DOCKER_TAG }}
            ${{ env.IMAGE_REGISTRY }}/dev-hops-api:latest
          push: true
